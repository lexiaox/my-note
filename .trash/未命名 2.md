---
创建时间: 2025-12-24 15:56
tags:
  - 数学
status: 📝 编写中
---
函数连续性是微积分中的核心概念，用来描述函数图形在某一点或区间上是否“连绵不断”。简单来说，如果画这个函数的图像时笔可以不离开纸面，那么这个函数就是连续的。
在数学上，函数连续有严格的定义，分为在一点连续和在区间上连续。
1. 函数在一点连续的定义
这是最基础的定义。假设函数 f(x) 在点 x_0 的某个邻域内有定义，如果同时满足以下三个条件，我们称函数 f(x) 在点 x_0 处连续：
 * 有定义：函数在 x_0 处有定义，即 f(x_0) 存在。
 * 有极限：当 x 趋近于 x_0 时，函数的极限存在，即 \lim_{x \to x_0} f(x) 存在。
 * 极限等于函数值：极限值等于该点的函数值，即 \lim_{x \to x_0} f(x) = f(x_0)。
公式表达：

如果上述任一条件不满足，点 x_0 就是函数的间断点（不连续点）。
更严格的 \varepsilon-\delta 定义（数学分析语言）
对于任意给定的正数 \varepsilon > 0，都存在一个正数 \delta > 0，使得当 |x - x_0| < \delta 时，都有 |f(x) - f(x_0)| < \varepsilon。这从量化的角度描述了“当 x 足够接近 x_0 时，f(x) 也无限接近 f(x_0)”。
2. 函数在区间上连续的定义
理解了单点连续后，就可以扩展到整个区间：
 * 开区间连续：如果函数 f(x) 在开区间 (a, b) 内的每一个点都连续，则称该函数在开区间 (a, b) 上连续。
 * 闭区间连续：对于闭区间 [a, b]，除了要求在内部开区间 (a, b) 上连续外，还必须满足端点连续性：
   * 在左端点 a 处右连续：\lim_{x \to a^+} f(x) = f(a)
   * 在右端点 b 处左连续：\lim_{x \to b^-} f(x) = f(b)
如果满足以上所有条件，则称函数在闭区间 [a, b] 上连续，这样的函数被称为连续函数。
3. 直观判断（举例）
为了更好理解，可以对比一下连续与不连续的情况：
 * 连续函数示例：
   * 多项式函数（如 y = x^2）
   * 正弦、余弦函数（如 y = \sin x）
   * 指数函数（如 y = e^x）
   * (这些图像都是连贯的曲线，没有断裂)
 * 不连续（间断）的常见情况：
   * 空心点（可去间断点）：虽然极限存在，但该点没有定义，或者定义值不在曲线上。
   * 跳跃（跳跃间断点）：函数图像在某一点突然“断开”并跳到另一个高度（如分段函数）。
   * 无限延伸（无穷间断点）：函数在某点趋向于无穷大（如 y = 1/x 在 x=0 处）。
总结
判断一个函数在某点是否连续，只需要检查：极限值是否存在，以及极限值是否等于该点的函数值。
函数连续性是微积分中的核心概念，用来描述函数图形在某一点或区间上是否“连绵不断”。简单来说，如果画这个函数的图像时笔可以不离开纸面，那么这个函数就是连续的。
在数学上，函数连续有严格的定义，分为在一点连续和在区间上连续。
1. 函数在一点连续的定义
这是最基础的定义。假设函数 f(x) 在点 x_0 的某个邻域内有定义，如果同时满足以下三个条件，我们称函数 f(x) 在点 x_0 处连续：
 * 有定义：函数在 x_0 处有定义，即 f(x_0) 存在。
 * 有极限：当 x 趋近于 x_0 时，函数的极限存在，即 \lim_{x \to x_0} f(x) 存在。
 * 极限等于函数值：极限值等于该点的函数值，即 \lim_{x \to x_0} f(x) = f(x_0)。
公式表达：

如果上述任一条件不满足，点 x_0 就是函数的间断点（不连续点）。
更严格的 \varepsilon-\delta 定义（数学分析语言）
对于任意给定的正数 \varepsilon > 0，都存在一个正数 \delta > 0，使得当 |x - x_0| < \delta 时，都有 |f(x) - f(x_0)| < \varepsilon。这从量化的角度描述了“当 x 足够接近 x_0 时，f(x) 也无限接近 f(x_0)”。
2. 函数在区间上连续的定义
理解了单点连续后，就可以扩展到整个区间：
 * 开区间连续：如果函数 f(x) 在开区间 (a, b) 内的每一个点都连续，则称该函数在开区间 (a, b) 上连续。
 * 闭区间连续：对于闭区间 [a, b]，除了要求在内部开区间 (a, b) 上连续外，还必须满足端点连续性：
   * 在左端点 a 处右连续：\lim_{x \to a^+} f(x) = f(a)
   * 在右端点 b 处左连续：\lim_{x \to b^-} f(x) = f(b)
如果满足以上所有条件，则称函数在闭区间 [a, b] 上连续，这样的函数被称为连续函数。
3. 直观判断（举例）
为了更好理解，可以对比一下连续与不连续的情况：
 * 连续函数示例：
   * 多项式函数（如 y = x^2）
   * 正弦、余弦函数（如 y = \sin x）
   * 指数函数（如 y = e^x）
   * (这些图像都是连贯的曲线，没有断裂)
 * 不连续（间断）的常见情况：
   * 空心点（可去间断点）：虽然极限存在，但该点没有定义，或者定义值不在曲线上。
   * 跳跃（跳跃间断点）：函数图像在某一点突然“断开”并跳到另一个高度（如分段函数）。
   * 无限延伸（无穷间断点）：函数在某点趋向于无穷大（如 y = 1/x 在 x=0 处）。
总结
判断一个函数在某点是否连续，只需要检查：极限值是否存在，以及极限值是否等于该点的函数值。
