好的，没问题！`in2csv` 是一个非常实用的工具，它是 **`csvkit`** 命令行工具集的一部分。这个工具集专门用于在命令行下处理 CSV 文件，但 `in2csv` 的主要功能就是解决我们将 **非 CSV 格式** 的文件（比如 XLS、XLSX、JSON、HTML 表格等）转换为 **标准 CSV 格式** 的问题。

我会为您详细讲解 `in2csv` 的用法，并提供可以直接复制的版本供您保存。

---



### 1. 核心功能与用法

`in2csv` 的基本语法非常简洁，核心思想是将输入文件通过 **管道 `|`** 或直接 **重定向 `>`** 输出到标准 CSV 格式。

#### 基本语法：

Bash

```
in2csv [选项] [输入文件路径]
```

**示例 (您之前成功的操作):**

Bash

```
in2csv "table-1.xls" > "cleaned_data.csv"
```

这行命令的作用是：读取 **`table-1.xls`** 文件，将其内容解析并转换为 CSV 格式，然后将转换后的结果输出并保存到 **`cleaned_data.csv`** 文件中。

---

### 2. 常用选项（处理 Excel/XLSX 文件）

在处理 Excel 文件时，由于 Excel 工作簿中可能包含多个工作表、多行标题甚至隐藏的行，您需要使用以下选项来精确地指定要转换的数据：

| **选项**                      | **作用**                                       | **示例**                              |
| --------------------------- | -------------------------------------------- | ----------------------------------- |
| **`-f FILENAME_EXTENSION`** | **强制指定文件格式**（当文件后缀名不标准时）。                    | `in2csv -f xls data.txt`            |
| **`--sheet SHEET_NAME`**    | **指定工作表名称**。如果 Excel 文件有多个工作表，必须使用此选项。       | `in2csv --sheet "Sheet1" data.xlsx` |
| **`--names`**               | **显示工作簿中的所有工作表名称**。用于确定要转换的表名。               | `in2csv --names data.xlsx`          |
| **`--skip-lines N`**        | **跳过文件开头的 N 行**。这在您的 FBI 数据中非常有用，可以跳过顶部的注释行。 | `in2csv --skip-lines 3 data.xls`    |
| **`--no-header-row`**       | **不输出标题行**。如果不需要列名，可以使用此选项。                  | `in2csv --no-header-row data.xls`   |

---

### 3. 常见高级应用示例

#### 示例 A: 查看 Excel 文件中有哪些工作表（方便选择）

Bash

```
in2csv --names my_financial_data.xlsx
```

#### 示例 B: 转换指定工作表并跳过注释行

假设您的 Excel 文件名为 `data.xlsx`，您想转换名为 "Crime_Data" 的工作表，并跳过前 4 行注释：

Bash

```
in2csv --sheet "Crime_Data" --skip-lines 4 data.xlsx > filtered_crime.csv
```

#### 示例 C: 转换其他格式（例如 JSON 文件）

`in2csv` 不仅限于 Excel，它还可以转换其他结构化数据，如 JSON、DBF (数据库文件) 等。

Bash

```
in2csv data.json > data.csv
```

---

## 📝 可复制的版本 (供笔记使用)

```
# in2csv 核心功能：将非 CSV 文件转换为标准 CSV 格式

# 1. 基本转换（适用于您的 FBI 文件）
# 将 XLS 文件转换为 CSV 文件
in2csv "table-1.xls" > "cleaned_data.csv"

# 2. 转换指定工作表 (如果 Excel 文件中有多个工作表)
in2csv --sheet "Sheet Name" "my_workbook.xlsx" > "sheet_output.csv"

# 3. 转换时跳过开头的 N 行（用于跳过文件注释/多余标题）
# 跳过文件开头的 3 行，然后转换为 CSV
in2csv --skip-lines 3 "data.xlsx" > "no_comments.csv"

# 4. 查看 Excel 文件中有哪些工作表名称
in2csv --names "multi_sheet_file.xlsx"

# 5. 转换 JSON 文件为 CSV 文件
in2csv "data.json" > "data_as_csv.csv"
```