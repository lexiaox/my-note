---
创建时间: 2025-12-24 15:58
tags:
  - 数学
  - 函数
  - 连续性
status: 📝 编写中
---


**Tags:** #Math/Calculus #Definition #Analysis
**Date:** 2025-12-24

---

## 1. 几何直观
* **通俗理解**：画函数图像时，笔尖可以**不离开纸面**画完一段曲线。
* **反例**：图像出现断裂、空洞或跳跃。

## 2. 严格数学定义 (在一点 $x_0$ 处)

### 2.1 "三要素" 定义法 (常用)
函数 $f(x)$ 在点 $x_0$ 处连续 $\iff$ 满足以下三个条件：
1.  **在点 $x_0$ 有定义** (即 $f(x_0)$ 存在)。
2.  **极限存在** (即 $\lim_{x \to x_0} f(x)$ 存在，意味着左极限=右极限)。
3.  **极限值等于函数值** ($\lim_{x \to x_0} f(x) = f(x_0)$)。

> [!failure] 常见间断原因 (Debug Guide)
> * **$f(x)$ 无定义** $\rightarrow$ 如 $f(x)=1/x$ 在 $x=0$。
> * **极限不存在** $\rightarrow$ 如跳跃间断点 (Jump Discontinuity)。
> * **极限 $\neq$ 函数值** $\rightarrow$ 可去间断点 (Removable Discontinuity)。

### 2.2 $\varepsilon-\delta$ 定义 (数学分析语言)
这实际上是量化了“无限接近”的概念，常用于理论证明。

对于任意给定的 $\varepsilon > 0$ (误差容忍度)，总存在 $\delta > 0$ (输入范围)，使得：
$$\forall x: |x - x_0| < \delta \implies |f(x) - f(x_0)| < \varepsilon$$

---

## 3. 区间上的连续性

* **开区间 $(a, b)$**：若 $f(x)$ 在区间内**每一点**都连续。
* **闭区间 $[a, b]$**：
    1.  在内部开区间 $(a, b)$ 连续。
    2.  在左端点 $a$ **右连续** ($\lim_{x \to a^+} f(x) = f(a)$)。
    3.  在右端点 $b$ **左连续** ($\lim_{x \to b^-} f(x) = f(b)$)。

---

## 4. Developer's Perspective (CS 视角)

对于计算机专业，连续性可以理解为 **"Robustness" (鲁棒性)** 或 **"Smoothness" (平滑性)**：

1.  **浮点数陷阱**：计算机中不存在真正的“连续”，所有数据都是离散的 (Discrete)。我们在屏幕上看到的“连续曲线”实际上是极其密集的离散点采样。
2.  **插值 (Interpolation)**：如果假设数据是来自一个连续函数，我们可以安全地在两个采样点之间进行线性插值或样条插值。如果不连续（如阶跃信号），插值会产生巨大误差（如吉布斯现象）。
3.  **代码类比**：
    ```c
    // 连续性检查的伪代码逻辑
    // 如果输入变化很小 (delta_x)，输出变化也必须限制在一定范围内 (epsilon)
    if (abs(input_new - input_old) < DELTA) {
        assert(abs(output_new - output_old) < EPSILON);
    }
    ```

---
**Related Notes:** [[极限的定义]], [[间断点的分类]], [[介值定理]]
