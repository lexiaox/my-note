在C语言中，字符串本质上是以 **空字符 (`\0`)** 结尾的字符数组。处理这些字符串的核心函数大多包含在头文件 `<string.h>` 中。

为了方便你做笔记，我将这些函数按**功能分类**整理，并附上了关键的**注意事项**。

---

### 1. 基础与长度计算

这是最常用的起点。记住，C语言中的字符串长度不包含末尾的 `\0`。

| **函数名**      | **原型简述**                       | **功能描述** | **笔记重点**                         |
| ------------ | ------------------------------ | -------- | -------------------------------- |
| **`strlen`** | `size_t strlen(const char *s)` | 计算字符串长度  | **不包含**结束符 `\0`。例如 `"abc"` 返回 3。 |

> **⚠️ 注意：** `sizeof` 和 `strlen` 是不同的。`sizeof` 计算数组占用的总字节数（包含 `\0`），而 `strlen` 只计算有效字符数。

---

### 2. 字符串复制与拼接 (Copy & Concatenate)

这部分操作涉及内存写入，是最容易出现**缓冲区溢出 (Buffer Overflow)** 的地方，需格外小心。

|**函数名**|**原型简述**|**功能描述**|**笔记重点**|
|---|---|---|---|
|**`strcpy`**|`char *strcpy(char *dest, const char *src)`|将 `src` 复制到 `dest`|会连同 `\0` 一起复制。**不安全**，若 `dest` 空间不足会溢出。|
|**`strncpy`**|`char *strncpy(char *dest, const char *src, size_t n)`|复制前 `n` 个字符|**更安全**。但如果 `src` 长度 $\ge n$，则 `dest` **不会**自动添加 `\0`，需手动添加。|
|**`strcat`**|`char *strcat(char *dest, const char *src)`|将 `src` 拼接到 `dest` 后面|会覆盖 `dest` 原有的 `\0`。确保 `dest` 剩余空间足够！|
|**`strncat`**|`char *strncat(char *dest, const char *src, size_t n)`|拼接前 `n` 个字符|自动在末尾添加 `\0`，比 `strcat` 安全。|

---

### 3. 字符串比较 (Comparison)

用于判断字符串是否相等或排序。

|**函数名**|**原型简述**|**功能描述**|**返回值含义 (重要)**|
|---|---|---|---|
|**`strcmp`**|`int strcmp(const char *s1, const char *s2)`|比较两个字符串|**0**: 相等<br><br>  <br><br>**<0**: s1 小于 s2<br><br>  <br><br>**>0**: s1 大于 s2|
|**`strncmp`**|`int strncmp(const char *s1, const char *s2, size_t n)`|比较前 `n` 个字符|同上，用于比较前缀。|

> **💡 记忆技巧：** 返回值就像做减法 ($s1 - s2$)。如果 $s1$ ASCII码小，结果就是负数。

---

### 4. 字符串搜索 (Searching)

用于在字符串中查找特定的字符或子串。

- **`strchr(const char *s, int c)`**:
    
    - **功能**: 在字符串 `s` 中查找字符 `c` **第一次**出现的位置。
        
    - **返回**: 指向该位置的指针；若找不到返回 `NULL`。
        
- **`strrchr(const char *s, int c)`**:
    
    - **功能**: 查找字符 `c` **最后一次**出现的位置（Reverse search）。
        
- **`strstr(const char *haystack, const char *needle)`**:
    
    - **功能**: 在 `haystack` 中查找子串 `needle` 第一次出现的位置。
        
    - **返回**: 指向子串起始位置的指针；若找不到返回 `NULL`。
        

---

### 5. 类型转换与格式化 (Conversion & Formatting)

这些函数通常在 `<stdlib.h>` (转换) 和 `<stdio.h>` (格式化) 中。

#### 字符串转数字 (`<stdlib.h>`)

- **`atoi(const char *s)`**: String to Integer (转为 `int`)。
    
- **`atof(const char *s)`**: String to Float (转为 `double`)。
    
- **`atol(const char *s)`**: String to Long (转为 `long`)。
    

#### 格式化字符串 (`<stdio.h>`)

这是一个非常强大的工具，用于将数字、字符混合组合成一个新的字符串。

- **`sprintf(char *str, const char *format, ...)`**:
    
    - **功能**: 与 `printf` 类似，但不是输出到屏幕，而是**打印到字符串 `str` 中**。
        
    - **示例**: `sprintf(buffer, "Score: %d, Rate: %.2f", 100, 0.5);`
        

---

### 6. 综合代码示例 (Cheat Sheet)

你可以把这段代码抄在笔记旁边作为用法参考：

C

```
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main() {
    char str1[20] = "Hello";
    char str2[] = "World";
    char buffer[50];

    // 1. 拼接
    strcat(str1, " ");     // str1 变成 "Hello "
    strcat(str1, str2);    // str1 变成 "Hello World"

    // 2. 长度
    printf("Length: %zu\n", strlen(str1)); // 输出 11

    // 3. 复制 (使用较安全的 strncpy)
    strncpy(buffer, str1, sizeof(buffer) - 1);
    buffer[sizeof(buffer) - 1] = '\0'; // 确保手动截断

    // 4. 比较
    if (strcmp(buffer, "Hello World") == 0) {
        printf("Strings are equal.\n");
    }

    // 5. 查找
    char *ptr = strstr(buffer, "World"); // 找到 "World" 的起始位置
    if (ptr != NULL) {
        printf("Found substring at index: %ld\n", ptr - buffer);
    }
    
    // 6. 格式化输出到字符串
    char result[50];
    int score = 98;
    sprintf(result, "User %s got %d points", "Alice", score);
    printf("%s\n", result);

    return 0;
}
```

---

### 给你的笔记建议：

1. **安全第一**：在笔记重点标记 `strncpy` 和 `strncat` 比它们的不带 `n` 的版本更安全，但在工程中通常推荐使用更现代的安全函数（如 `snprintf`）。
    
2. **指针思维**：很多函数（如 `strstr`）返回的是**指针**，而不是索引。如果你需要索引，记得用 `返回指针 - 原字符串首地址`。
    

**你要整理一份关于“如何避免C语言字符串缓冲区溢出”的高级笔记吗？这通常是面试中的加分项。**

温馨提示：如要解锁所有应用的完整功能，请开启 [Gemini 应用活动记录](https://myactivity.google.com/product/gemini?authuser=1)。